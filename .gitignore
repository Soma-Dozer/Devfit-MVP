# ── secrets (NEVER commit) — 어느 깊이의 폴더든 적용 ──
.env
.env.*
*.env
!.env.example

# ── OS ──
.DS_Store

# ── Java / Gradle ──
.gradle/
build/

# ── H2 / 로컬 DB 데이터 (어느 깊이든) ──
*.mv.db
*.lock.db
**/data/

# ── Node / Next.js ──
node_modules/
.next/
out/
.vercel
*.tsbuildinfo
next-env.d.ts

# ── logs ──
*.log
